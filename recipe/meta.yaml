{% set name = "ocean_data_gateway" %}
{% set version = "0.7" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/ocean_data_gateway-{{ version }}.tar.gz
  sha256: 274cfb2351abf3a96c7ca57a54bd8fe0aec0e5c87e6c2b15672f78b7c2fe5fd3

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
    - setuptools-scm
  run:
    - aiohttp
    - cf_xarray
    - dask-core
    - erddapy
    - flake8
    - h5netcdf
    - intake
    - intake-xarray
    - ioos_qc
    - ipython
    - isort
    - joblib
    - jupyter
    - jupyterlab
    - matplotlib-base
    - netcdf4
    - numpy
    - pandas
    - pint
    - pint-pandas
    - pint-xarray
    - pytest
    - python >=3.6
    - requests
    - scipy
    - shapely
    - xarray

test:
  imports:
    - ocean_data_gateway
    - ocean_data_gateway.readers
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/axiom-data-science/ocean_data_gateway
  summary: Your gateway to ocean data.
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - kthyng
